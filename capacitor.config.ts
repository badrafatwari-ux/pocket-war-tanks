import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.firjaapp.retrotankduel',
  appName: 'pocket-war-tanks',
  webDir: 'dist',
};

export default config;
